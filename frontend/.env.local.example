# Dashboard / classement : true = mocks (plan B), false = vraies routes C
NEXT_PUBLIC_USE_MOCKS=true

# URLs des services (rewrites Next)
BACKEND_URL=http://localhost:5000
IA_URL=http://localhost:4000
